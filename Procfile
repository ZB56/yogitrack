# Tells Heroku what to run. "web" is the process type that receives HTTP
# traffic; Heroku sets PORT in the environment and server.js reads it.
# No build command here: that is heroku-postbuild in package.json, which runs
# before this process starts.
web: node server/server.js
